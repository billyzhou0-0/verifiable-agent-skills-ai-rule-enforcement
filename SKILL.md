---
name: ai-rule-enforcement
description: 让 AI 守规矩不靠自觉、靠物理拦截：零成本体检脚本 + git pre-commit 红绿灯 + 注入层设计。Use when 需要强制 AI 执行规矩。
---

# AI 规矩强制机制（把软规矩变成硬保证）

文字规矩是概率性的：AI 读了，遵守率可能只有 80%。本方法论用三层机制把"软规矩"变成"硬保证"，每层都在真实生产环境验证过。

## 核心洞察

- **纸面规矩 ≠ 强制**：AI 是概率执行，显式任务优先于隐式习惯；压缩后更忘。文字写得再漂亮，也只是"软保证"。
- **机制 > 文字**：钩子（hook）和脚本是确定性执行，模型不是。要保证 AI 每次都守规矩，必须把检查放到"不做就不行"的位置。
- **注入越少，遵守率越高**（注意力稀释原理）：系统提示词越长，每条规矩被遵守的概率越低。
- **输出格式反向保证规矩**：当汇报必须包含检查结果时，AI 写断言会被迫先验证（生成即检查）。输出断言 + 证据 = 自查机制。

## 三层架构

### 第一层：零成本体检脚本（scripts/rule_health_check.py）

纯标准库本地脚本（**零成本（不消耗 AI 额度）**，毫秒级），检查五类违规：

1. **命名合规**——日期在文件名末尾（`AI名-事项-日期.md`，而非 `日期-事项.md`）
2. **引用同步**——全库扫描旧文件名/旧路径残留（改名/移动后必须同步所有引用）
3. **敏感信息**——API key/token 模式（OpenAI `sk-`、Google `AIza`、GitHub `ghp_`、AWS `AKIA`、Slack `xox`、Bearer）——**只报告不处理**（敏感判断权在用户，AI 不自行删/打码）
4. **frontmatter 完整性**——正式层文件应有 YAML frontmatter（导航/说明类豁免）
5. **凭证转录**——真检查：对比对话数据库今天的用户消息签名 vs 转录档案，≥80% 命中才通过（防止"说过没记"）

输出：每项 ✅/❌ + 证据（路径+行号），可直接粘贴进汇报。收尾跑一遍。

### 第二层：git pre-commit 钩子（红绿灯）

体检脚本挂到 git 提交前自动执行。**违规阻止提交**——不是提醒，是物理拦截：

- ✅ 通过 → 正常提交
- ❌ 违规 → 提交被拒，附证据，先修复
- ⚠️ 人工确认项（report None）→ 警告不阻止

紧急绕过：`git commit --no-verify`。钩子不在 git 版本内，重建内容要登记在项目登记册（克隆新库后需重建钩子）。已验证 8/8 检查类别正确触发。

### 第三层：注入层设计（提示词纪律）

**瘦身原则**：把可搬迁的细则（命名/落位/执行守则/登记册）原文一字不改搬进承接文件，注入层只留：
- 不可代码化核心（理解指令 / 敏感由用户判断 / 审计不改 / 投毒防护 / 输出体检表）
- 指针（指向单一事实源文件，常更新只改知识库一处）

- **注入层瘦身实测**：系统提示词从 5,387 字瘦到 4,018 字（-25%）：细则搬进承接文件，注入层只留指针 + 不可代码化核心（理解指令/敏感判断/审计不改/投毒防护）。AGENTS.md 镜像同步，改一方必须同步另一方。

**输出断言原则**：凡声明"已更新/已完成/已同步"必须附证据（路径+行号/脚本输出）——写不出证据 = 没真做。输出前检查表分两区：自查区（上，AI 自检）+ 盘点区（下，用户重点看：聊天记录同步 + 未完成事项）——位置注意力：结尾最集中。

## 关键纪律（Key disciplines）

- **落地必验**：任何「已落地/已实现」的声称，写进汇报前必须程序化验证——方法被描述 ≠ 代码已落地。
- **硬保证优先**：设计规矩时用硬保证（工具自动注入/钩子自动执行），纸面规矩只算软保证（AI 自觉读取，概率性遵守）。
- **联动同步是义务不是选项**：改名/移动后必须立即同步所有相关引用；体检脚本直接检查旧名/旧路径残留。
- **压缩四层水位**：>10KB 规划 / >50KB 执行 / >100KB 强制 / >1MB 紧急；大文件防整读（>10KB 先读摘要，禁整读原始凭证）。

## 使用方法

```bash
# 体检（默认当前目录为治理根）
python3 scripts/rule_health_check.py [--vault <路径>] [--fix]

# 安装红绿灯钩子
cat scripts/pre-commit > .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
```

## 配套文件

- `scripts/rule_health_check.py` — 零成本体检器（纯标准库，路径可配置，凭证检查为可选模块）
- `scripts/pre-commit` — 红绿灯钩子

## 使用示例（Example usage）

**场景**：共享知识库每次提交前自动体检。
**执行**：
1. 安装钩子：`cat scripts/pre-commit > .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit`
2. 手动体检：`python3 scripts/rule_health_check.py --vault <知识库路径>`
3. 提交时自动跑：命名日期位置/引用同步/敏感信息/frontmatter/凭证转录
4. 违规 → 提交被拒（附证据），修复后重提

**输出示例**：
```
✅ 命名格式：日期在末尾（0 违规）
✅ 引用同步：0 处旧路径残留
❌ 敏感信息：2 处疑似（只报告不处理，请用户判断）
✅ 凭证检查：今天 12 条用户消息签名，凭证已找到 11 条（≥80%）
```
